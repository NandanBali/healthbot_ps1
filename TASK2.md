Phase 2: High-Risk Deterministic Guardrails[ ] Task 2.1: Build app/tools/emergency_triage.py. Create a plain-text keyword matching function check_emergency_triggers(user_text: str) -> bool.Check for acute high-risk phrases: ["chest pain", "crushing pressure", "left arm pain", "heart attack", "can't breathe"].[ ] Task 2.2: In the main message routing logic, run this string check before processing any file or sending text to the LLM. If triggered, immediately bypass all tools/models and display a high-visibility red emergency prompt: st.error("🚨 EMERGENCY LOGGED: Please call emergency services (911/102) immediately.").Phase 3: DEDICATED TASK - ECG Visual Analysis Tool (Image/PDF)[ ] Task 3.1: Create the File Preprocessing Module (app/utils/file_processor.py)Write a function convert_file_to_image(uploaded_file) that checks the file extension.If it's a JPEG/PNG, open it directly using Pillow (Image.open).If it's a PDF, use pdf2image.convert_from_bytes(uploaded_file.read()) to convert the first page into a Pillow image object.Save or buffer the resulting image and encode it as a Base64 string so it can be passed natively to a multimodal API.[ ] Task 3.2: Implement the Vision Analysis Tool (app/tools/ecg_vision_tool.py)Define a function analyze_ecg_visual(base64_image: str) -> dict.Inside this function, make a direct API call to a multimodal model (like gpt-4o, claude-3-5-sonnet, or gemini-2.5-flash).Use a highly structured, strict developer/system prompt for the vision model:"You are an expert diagnostic assistant examining a visual ECG strip or automated report image. Extract any explicitly printed text metrics (e.g., 'HR: 82 BPM', 'PR Interval', 'Sinus Rhythm'). If no text parameters are printed, visually estimate if the QRS complexes appear regularly spaced or abnormally irregular. Return a strict JSON response with keys: 'estimated_bpm' (int or null), 'rhythm_status' ('Normal', 'Irregular', 'Unclear'), and 'visual_summary' (brief textual breakdown)."[ ] Task 3.3: Format the OutputEnsure the tool returns clean, structured data back to the master agent:JSON{
"heart_rate_bpm": 78,
"rhythm_classification": "Normal Sinus Rhythm",
"observations": "Clear regular QRS spikes visible on grid lines. No extreme irregularities noted textually on the chart snapshot."
}
Phase 4: Complementary Health Scorer Tool[ ] Task 4.1: Programmatic Risk Calculator (app/tools/risk_tool.py)Create a deterministic function calculate_cardiac_risk(age: int, systolic_bp: int, smokes: bool) -> str.Implement basic scoring arithmetic:Age > 55 $\rightarrow$ +2 pointsSystolic BP > 140 $\rightarrow$ +3 pointsSmokes == True $\rightarrow$ +2 pointsMap total points to risk tiers: 0-2 = "Low Risk", 3-4 = "Moderate Risk", 5+ = "High Risk". Return a concise summary string.Phase 5: Master Agent Integration & Loop[ ] Task 5.1: Bind the toolset (analyze_ecg_visual, calculate_cardiac_risk) to the primary conversational LLM using native function-calling schemas.[ ] Task 5.2: Set up the tool orchestration framework. If a user uploads an ECG file, configure the system to automatically trigger convert_file_to_image, execute analyze_ecg_visual, and hand the raw structural findings back to the chat model to explain to the user in human-friendly terms.[ ] Task 5.3: Run an integration test matrix for three distinct user behaviors:Text Input: "I'm having severe crushing chest pain right now" $\rightarrow$ Ensure immediate emergency lockout flags.Form Input: User enters parameters (58, 145 BP, smoker) $\rightarrow$ Ensure it executes the calculator tool perfectly.File Upload: User uploads a PDF/PNG ECG printout $\rightarrow$ Verify the image is converted to base64, sent to the vision model, and the chatbot summarizes the rhythm status smoothly."""
